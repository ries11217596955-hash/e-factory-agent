name: site-auditor-fixed-list

on:
  workflow_dispatch:
    inputs:
      audit_mode:
        description: 'Manual audit mode'
        required: true
        default: 'REPO'
        type: choice
        options:
          - REPO
          - URL
          - UNIFIED
      base_url:
        description: 'Base URL for URL or UNIFIED mode'
        required: false
        default: 'https://automation-kb.pages.dev'
        type: string
  push:
    paths:
      - 'agents/gh_batch/site_auditor_cloud/input/inbox/**'

jobs:
  site-audit:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout agent repo
        uses: actions/checkout@v4

      - name: Checkout target repo for REPO / UNIFIED audit
        if: github.event_name == 'workflow_dispatch' && (inputs.audit_mode == 'REPO' || inputs.audit_mode == 'UNIFIED')
        uses: actions/checkout@v4
        with:
          repository: ries11217596955-hash/automation-kb
          token: ${{ secrets.GH_BATCH_PAT }}
          path: target_repo

      - name: Setup Node for URL / UNIFIED mode
        if: github.event_name == 'workflow_dispatch' && (inputs.audit_mode == 'URL' || inputs.audit_mode == 'UNIFIED')
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: Install Node deps for URL / UNIFIED mode
        if: github.event_name == 'workflow_dispatch' && (inputs.audit_mode == 'URL' || inputs.audit_mode == 'UNIFIED')
        run: |
          cd agents/gh_batch/site_auditor_cloud
          npm install

      - name: Install Playwright browsers for URL / UNIFIED mode
        if: github.event_name == 'workflow_dispatch' && (inputs.audit_mode == 'URL' || inputs.audit_mode == 'UNIFIED')
        run: |
          cd agents/gh_batch/site_auditor_cloud
          npx playwright install --with-deps chromium

      - name: Run SITE_AUDITOR
        env:
          FORCE_MODE: ${{ github.event_name == 'push' && 'ZIP' || inputs.audit_mode }}
          TARGET_REPO_PATH: ${{ github.workspace }}/target_repo
          BASE_URL: ${{ github.event_name == 'workflow_dispatch' && inputs.base_url || 'https://automation-kb.pages.dev' }}
        run: |
          cd agents/gh_batch/site_auditor_cloud
          pwsh ./run.ps1

      - name: Debug tree
        if: always()
        run: |
          ls -R agents/gh_batch/site_auditor_cloud || true
          ls -R target_repo || true

      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: audit-output
          path: |
            agents/gh_batch/site_auditor_cloud/outbox/**
            agents/gh_batch/site_auditor_cloud/reports/**
          if-no-files-found: warn
