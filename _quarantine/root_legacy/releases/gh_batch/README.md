# GH_BATCH Releases

Use this directory only for packaged release ZIP files if you intentionally keep release binaries in git.

Default rule:
- source of truth = `agents/gh_batch/`
- release ZIP = deliverable, not source
