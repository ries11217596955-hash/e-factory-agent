{
  "schema_version": "1.0.0",
  "catalog_id": "site_auditor_v3_universal_capability_discovery",
  "selection_policy": {
    "mode": "priority_then_readiness",
    "scope": "universal_site_auditor",
    "forbidden": [
      "select_target_specific_finding_repairs_as_product_roadmap",
      "repeat_a_capability_already_selected_by_current_discovery_run",
      "emit_no_selection_when_ready_candidates_exist"
    ]
  },
  "candidates": [
    {
      "capability_id": "repair_execution_layer",
      "capability_class": "universal_execution",
      "status": "READY_TO_BUILD",
      "priority": 100,
      "trigger_when_next_capability": "capability_discovery",
      "selection_reason": "Session finalization now emits a structured FINAL_ACTION_PLAN; the universal auditor needs a safe repair/execution layer that can consume generic final action plans without becoming target-specific.",
      "why_universal": "Any audited target can produce a structured final action plan, so the next product layer must operate on the plan contract rather than on one test site's findings.",
      "required_repo_truth": [
        "session_aggregation_and_finalization_proven",
        "FINAL_ACTION_PLAN_contract_exists",
        "self_build_truth_alignment_proven"
      ],
      "task_contract": {
        "task_type": "BUILD_CAPABILITY",
        "state_key": "repair_execution_layer",
        "required_fields": [
          "capability_id",
          "build_status",
          "plan_contract",
          "safety_gate",
          "validation"
        ]
      }
    }
  ]
}
